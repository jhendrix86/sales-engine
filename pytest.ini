[pytest]
# Put the repo root on sys.path so bare `pytest` (CI) can import app/ -
# without this tests/conftest.py fails: ModuleNotFoundError: No module named "app".
pythonpath = .
asyncio_mode = auto
